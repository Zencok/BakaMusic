import * as frontend from "./service";
import defaultSheet from "./default-sheet";

const MusicSheet = {
    // ...sheetsMethod,
    defaultSheet,
    frontend,
};

export default MusicSheet;
export { defaultSheet };
